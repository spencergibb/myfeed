spring:
  application:
    name: myfeed-router
  cloud:
    config:
      discovery:
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery.myfeed.com:11010/eureka/

